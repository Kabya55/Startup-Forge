export const metadata = {
  title: "Sign Up",
  description: "Create a StartupForge account to find your next opportunity or hire top-tier talent.",
};

export default function SignupLayout({ children }) {
  return children;
}
