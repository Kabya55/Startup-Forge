export const metadata = {
  title: "Login",
  description: "Log in to your StartupForge account to manage opportunities, apply for positions, or hire talent.",
};

export default function LoginLayout({ children }) {
  return children;
}
