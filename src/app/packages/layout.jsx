export const metadata = {
  title: "Pricing Packages | StartupForge",
  description: "View pricing packages on StartupForge. Find the perfect plan for opportunity collaborators and founders.",
};

export default function PackagesLayout({ children }) {
  return children;
}
